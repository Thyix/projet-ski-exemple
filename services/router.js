export const renderHomeScreen = (req, res) => {
  res.render("home.ejs");
};

export const renderLoginScreen = (req, res) => {
  res.render("login.ejs");
};

export const renderProfileScreen = (req, res) => {
  res.render("profile.ejs");
};
